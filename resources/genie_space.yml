# =============================================================================
# Genie Space: Natural language exploration of gold tables
# NOTE: Genie Spaces are not directly supported as a DABs resource type yet.
# This file documents how to manage Genie Spaces alongside your bundle
# and provides the REST API calls to automate their creation in CI/CD.
# =============================================================================

# --- Genie Space Configuration (for reference / automation scripts) ---
# genie_space:
#   display_name: "[${bundle.target}] Orders Analytics Genie Space"
#   description: >
#     Explore order analytics using natural language.
#     Ask questions about daily revenue, product categories, and customer lifetime value.
#   warehouse_id: ${var.warehouse_id}
#   tables:
#     - ${var.catalog}.${var.schema}.gold_daily_revenue
#     - ${var.catalog}.${var.schema}.gold_category_summary
#     - ${var.catalog}.${var.schema}.gold_customer_ltv
#   sample_questions:
#     - "What was the total revenue last week?"
#     - "Which product category generates the most revenue?"
#     - "Who are our top 5 customers by lifetime value?"
#     - "Show me daily order trends"

# --- To create a Genie Space via REST API in your CI/CD pipeline, use: ---
# POST /api/2.0/genie/spaces
# See: scripts/manage_genie_space.py
